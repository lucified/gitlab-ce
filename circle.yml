
machine:
  node:
    version: 6.1.0
  services:
    - docker

dependencies:
  override:
    - npm install -g ecs-upd8r@~0.1.6

test:
  override:
    - echo "No tests specified"

deployment:
  staging:
    branch: lucified
    commands:
      - ecs-upd8r

